{
  "name": "pg-wrapped",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {
    "elysia": "latest"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "scripts": {
    "build:client": "cd packages/client && bunx vite build --emptyOutDir --outDir ../../dist/client/",
    "build:server": "cd packages/server && bun build --compile --minify-whitespace --minify-syntax --target bun-linux-x64 --outfile ../../dist/server src/index.ts",
    "build": "concurrently bun:build:client bun:build:server && cp Caddyfile.deploy dist/Caddyfile"
  }
}
